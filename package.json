{
  "name": "learningdemos",
  "version": "1.0.0",
  "description": "Standalone interactive learning demos built without runtime Babel.",
  "scripts": {
    "build": "node scripts/build-runtime-js.js",
    "check": "node scripts/check-no-runtime-babel.js && node scripts/check-site-shell.js && node scripts/check-question-markup.js && node scripts/check-interactive-consistency.js",
    "test": "npm run build && npm run check",
    "serve": "node scripts/serve.js",
    "dev": "npm run build && npm run serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@babel/core": "^7.29.7",
    "@babel/plugin-transform-react-jsx": "^7.29.7",
    "postcss": "8.5.19",
    "tailwindcss": "3.4.17"
  }
}
